---
# Per environment values which override defaults in hmpps-x-ray-body-scans-api/values.yaml

generic-service:
  ingress:
    host: x-ray-body-scans-api.hmpps.service.justice.gov.uk

  env:
    SENTRY_ENVIRONMENT: prod
    HMPPS_AUTH_URL: "https://sign-in.hmpps.service.justice.gov.uk/auth"
    API_HMPPS_AUTH_BASE_URL: "https://sign-in.hmpps.service.justice.gov.uk/auth"
    API_PRISON_API_BASE_URL: "https://prison-api.prison.service.justice.gov.uk"

# CloudPlatform AlertManager receiver to route prometheus alerts to slack
# See https://user-guide.cloud-platform.service.justice.gov.uk/documentation/monitoring-an-app/how-to-create-alarms.html#creating-your-own-custom-alerts
generic-prometheus-alerts:
  alertSeverity: hmpps-connect-dps-prod
